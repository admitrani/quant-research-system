execution:
  mode: batch
  frequency: daily

storage:
  format: parquet
  partition_by: date

market_data:
  source: binance

  symbols:
    - BTCUSDT

  intervals:
    - 1h

  historical_start: "2020-01-01"

  ingestion:
    limit: 1000
    max_retries: 5
    backoff_seconds: 1.0
